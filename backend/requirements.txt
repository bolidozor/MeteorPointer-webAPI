# MeteorPointer API dependencies — pinned for reproducibility

# Core
Django==5.1.4
django-ninja==1.3.0
django-cors-headers==4.6.0

# ASGI server
uvicorn[standard]==0.34.0
gunicorn==23.0.0

# Database (psycopg 3)
psycopg[binary]==3.2.3

# Auth / crypto
PyJWT==2.10.1
cryptography==44.0.0

# Config
python-dotenv==1.0.1

# QR codes for web sign-in (pure Python, no deps)
segno==1.6.1

# Dev / test
pytest==8.3.4
pytest-django==4.9.0
ruff==0.8.6
