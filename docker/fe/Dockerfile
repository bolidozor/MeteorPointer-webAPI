# Minimal login-test frontend container (static files via nginx).
# The real frontend (MeteorPointer-FE / webUI) will replace this.
FROM nginx:1.27-alpine
COPY frontend/ /usr/share/nginx/html/
EXPOSE 80
